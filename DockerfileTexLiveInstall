FROM grandfleet/seng475-sde:4.0.13
RUN apt-get update && apt-get install python-dev -y
WORKDIR /sde
# Copy the installation files over
COPY / .
RUN rm -rf .git && ls 

# Make new directory for installation
RUN chmod +x installerTexLive && ./installerTexLive -d /usr/local/sde -e default -f || true